// z vars
if (respawn) {
	z = 2000;
} else {
	z = 128; /*z pos*/
}
ht = 64; // height (for offset)

direction = dir;

// create and set block
block = noone;
alarm[0] = 4; // to make sure block vars are right